pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.5.0'

    id 'org.ajoberstar.git-publish' version "${gradleGitPublishVersion}"
}

rootProject.name = 'untitled1'
